prefix pmlj:       <http://inference-web.org/2.0/pml-justification.owl#>
prefix conversion: <http://purl.org/twc/vocab/conversion/>

select (count(distinct ?s) as ?count)
where {
   ?s a pmlj:InferenceStep
}

